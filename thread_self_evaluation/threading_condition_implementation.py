"""
Condition (threading.Condition)
🔸 Problem Statement: Producer-Consumer with Notification
Implement a producer-consumer problem using a shared queue (list). The producer thread produces numbers and notifies the consumer. The consumer thread waits until the queue is not empty, then consumes and prints the number. Use Condition to signal and wait properly.

✅ Focus: Wait-notify pattern using Condition.


"""
